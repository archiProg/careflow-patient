import { Text, View } from "react-native";

const LoginFaceComp = () => {
    return (
        <View className="flex-1 w-full h-full items-center justify-center">
            <Text>LoginFaceComp</Text>
        </View>
    );
};

export default LoginFaceComp;
